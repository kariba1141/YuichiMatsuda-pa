# ETRCARS
## 目次
To Be Continued.

## 1. 背景・目的
### 1.1. Why: なぜ行うのか？
現在のサービスステーションでは、車両情報や顧客情報を Excel ファイルや紙で管理している。そのため、情報の散在、更新漏れ、属人化などの課題が生じている。
また、他業務の増加により、現行の運用方法では対応が難しくなってきている。
さらに、個人情報保護法の改正により、顧客情報を適切に管理する体制を構築する必要がある。

### 1.2. What: 何を？
新しい顧客情報管理システムを導入することで、情報を一元化し、営業活動の効率を向上させ、情報の正確性向上を図る。
さらに、セキュリティとアクセス管理を強化することで、法令に準拠した情報管理システムの実現を図る。

## 2. 性能要件
### 2.1. 一般要件
本セクションでは、システム性能に関する一般要件として、同時ユーザー数、総ユーザー数、応答時間を定義する。

#### 2.1.1. 同時ユーザー数
- 通常負荷：約 100 リクエスト／分
- ピーク負荷：約 250 リクエスト／分

#### 2.1.2. 総ユーザー数
- 初期ライセンスユーザー数：約 1,000 ユーザー

#### 2.1.3.
- 通常負荷：約 1〜3 秒
- ピーク負荷：約 7 秒

### 2.2. 高負荷が発生するポイント
本セクションでは、システムに高負荷が発生すると想定される特定の機能やシナリオを明確にします。
各ポイントについて、定められた性能目標を満たしていることを確認するため、性能テストを必ず実施する必要がある。

#### 2.2.1. 高負荷シナリオ 1：バッチデータのインポート／エクスポート
- 目標処理完了時間：約 95％ を 3 分以内 に処理完了（2,000 レコード）
- リトライ条件：処理が失敗した場合の自動リトライ条件は現時点ではない。
エラー発生時はユーザーにエラーメッセージが表示され、ユーザーが処理をやり直す必要がある。
- 高負荷を引き起こす操作：
  - 顧客、注文、工場などに関する CSV／Excel の大量インポート
  - スケジュール／アドホックによる エクスポート処理
- 想定頻度：
  - インポート：約 500 回／月
  - エクスポート：約 1000 回／月
- オートスケーリングルール：
  - バッチ処理は Redis/RQ を使った バックグラウンドジョブで実行される
    - 既定の RQ ジョブタイムアウト：600秒
    - エクスポートの長時間処理のため 3600秒 に拡張
    - フォアグラウンドワーカー（API）のタイムアウト：6000秒
    - Cloudflare は 100秒のタイムアウトのため、クライアントはそれ以上待機しない
  - 中長期のリソース拡張計画
    - システムは以下を処理できる必要がある：1000 店舗のユーザー基盤、ピーク 100,000 リクエストをオートスケールにより処理可能
- 冗長化要件：
  - アプリケーション層：複数の ECS タスクを異なるサブネット／AZ に配置し ALB 配下で稼働
  - データベース：Amazon RDS PostgreSQL の Multi-AZ 構成と自動バックアップ
  - キャッシュ／キュー：ElastiCache Redis によるメモリキャッシュ
  - オブジェクトストレージ：S3 を使用し、大阪リージョンへのレプリケーションを設定
#### 2.2.2. 高負荷シナリオ 2：リアルタイムレポート／分析ダッシュボード
- リアルタイムレポートの目標応答時間：約 95％ を 3 分以内 に応答（2,000 レコード）
- 高負荷を引き起こす操作（Triggering Action）：多数のユーザーが同時に分析ページやダッシュボードへアクセスし、集計クエリやフィルタリングを実行することによる高負荷
- 想定頻度（Expected Frequency）：約 3000 ビュー／月
- オートスケーリングルール（Auto-scaling Rule）：
  - ECS のターゲットトラッキング（CPU 50% / メモリ 75%）を継続使用
  - 最低 3 タスク、最大 8 タスク
  - 持続的な高負荷時には上限拡張を想定
- ホットデータセットに対するクエリ最適化を適用
- 中長期のリソース拡張計画
  システムは以下に対応できる必要がある：
  - 1000 店舗のユーザー基盤
  - ピーク 100,000 リクエストをオートスケールで処理可能

### 2.3. バックアップの影響
本セクションでは、データバックアップおよびリカバリー操作がシステム性能に与える影響について定義。
バックアップ処理がシステム性能を不当に低下させないこと、またリカバリー目標が達成可能であることが重要。

#### 2.3.1. バックアップウィンドウ
- RDS の自動バックアップを 毎日 01:00 AM（日本時間）に取得
- 保持期間：7 日間
- 大阪リージョンへの クロスリージョンレプリケーションを実施
- S3 バケットも 東京 → 大阪へのレプリケーションを有効化

#### 2.3.2. 性能劣化
- データバックアップによる顕著な性能低下は発生しない

#### 2.3.3. リカバリー性能
- RTO（復旧時間目標）：約 3 時間
- RPO（復旧ポイント目標）：当日データの復旧が可能

### 2.4. サービスレベルアグリーメント

バグの軽減、予期せぬシステム障害の緩和、予期しないバグの防止

- バグ対応
  - 日本側から報告されたバグに対して、即時に修正対応を行う。
  - 修正を日本側へ報告する前に、完全な動作テストを実施する。テストには、QR スキャン、注文作成・更新、検索機能など、よく使用される機能の確認が含まれる。
  - CODIUM と日本側との間の透明性を高めるため、バグの根本原因の詳細な説明、実施した対処内容を記載した文書を提供し、将来的にバグ修正履歴を追跡できるようにする。
- 予期せぬシステム障害対応
  - 予期せぬサーバー障害が発生した場合、原因を特定するための徹底調査を行い、原因分析再発防止策を記載したポストモーテム（事後分析）文書を提供する。
  - CODIUM は、最悪の場合でも、同一原因による障害を再発させないこと、ダウンタイムが 1 時間を超えないことを保証する。
  - 日本側へ事前通知されていない予期せぬサーバー障害が、30 分以上継続し、そのような事象が 1 カ月以内に 3 回以上 発生した場合、MA（月額保守）費用に対して 15% の割引を適用する。
- 予期せぬバグ防止
  - QA チームが四半期ごとにシステムレビューを実施し、システム全体のテストを行う。
- バグおよび予期せぬシステム障害対応に関するサービスレベルアグリーメント


| サービスレベル | サポート時間 | レスポンス時間 | 解決時間（復旧時間） |
|----|----|----|----|
| Level 1：軽微インシデント | 営業時間内 | 3 時間以内 | 重大度に応じて変動 |
| Level 2：中程度インシデント | 営業時間内 | 3 時間以内 | 48 時間以内 |
| Level 3：重大インシデント | 24時間365日 | 1〜3 時間以内 | 3 時間以内 |

- Level 1：軽微インシデント
大きなシステム機能や利用に影響はないものの、解決が必要な小規模な問題を指す。
- Level 2：中程度インシデント
主要機能の一部が動作せず、業務に影響を与えるものの、システム全体が停止するほどではなく、迅速な対応が必要な問題を指す。
- Level 3：重大インシデント
サーバーがダウンしている、または業務に重大な支障をきたし、即時の復旧対応が必要な問題を指す。
- 24/7 の定義：
祝日を含む 1日24時間、週7日いつでも対応可能であることを指す。
- 合意された目標稼働率は設定していないが、当システムは月次ベースで最低でも 95％ の稼働率を維持するものとする。

### 2.5. 監視とアラート
このセクションでは、システム監視、しきい値設定、および通知条件の要件を定義する。

#### 2.5.1. インフラストラクチャの監視
- ECS:
  - CloudWatch ロググループと ECS メトリクスによるCPU/メモリアラート
  - CloudWatch メトリクスに基づく自動スケーリングトリガー
- EC2:
  - EC2 監視コンソールのCPU/メモリアラート
- ALB:
  - S3 に保存されたアクセス ログ (etrcars-alb-access-logs)
  - ALB ダッシュボードでの5xx エラー率の監視や合計リクエストの監視など
- RDS:
  - RDS コンソールでRDS CPU 使用率や解放可能なメモリなどを監視する
  - CloudWatchロググループ経由のエラーログ

#### 2.5.2. アプリケーション監視
- Sentry統合:
  - Sentry によるエラー追跡 (RAVEN_DSN)
  - Django 例外監視
- アラートチャンネル:
  - 重大（サーバーダウン）: Google チャットとメール経由でチーム全員

#### 2.5.3. 通知条件

| 監視対象項目 | 概要 | アラートのしきい値/条件 |
|----|----|----|
| パフォーマンス |  |  |
| CPU 使用率 | アプリケーション タスクの CPU 使用率が設定されたしきい値を超えているかどうかを監視する。 | 3 分連続で平均 50% 以上。 |
| メモリ使用率 | アプリケーション タスクのメモリ使用量が設定されたしきい値を超えているかどうかを監視する。 | 3 分連続で平均 75% 以上。 |
| CPU使用率 / 空きストレージ | データベースの空きストレージと CPU 容量を監視する。 | RDS FreeStorageSpace が 1 分間 1 GB 未満。CPU平均が 1 分間 50% 以上。 |
| 可用性 |  |  |
| URLヘルスチェック | ヘルスチェック URL が要求されると、期待される応答が取得されます。 | 20秒以内にHTTP 200 OKを返す。この条件が3回連続して満たされない場合は警告を発する。 |
| ログ監視 |  |  |
| アプリケーションエラー | アプリケーション ログでエラー メッセージを監視する。 | Sentryのアラート |
| 追加ログ | 前述の他にいくつかの追加ログ。 | 含まれるもの: ●	ALBアクセスログ ●	ECSタスクログ ●	ECS デプロイイベント ●	CloudWatch アラーム履歴 ●	RDS ログ ●	RDS メトリクス ログ ●	RDSスナップショット確認ログ ●	RDS自動バックアップ確認ログ ●	CloudTrailログ ●	S3 アクセスログ ● GuardDuty検出ログ ● AWS Inspector 検出レポートログ ●	Cloudflareネットワークログ ●	Cloudflare セキュリティ脅威ログ ● APIエラーログ ● iPadアプリの操作ログ ● Web管理画面操作ログ ● VPCフローログ ● セッションマネージャーのアクセスログ ● サービスの Cloudwatch メトリクス ログ ● AWS 構成 ● AWS セキュリティハブ

## 3. セキュリティ要件
システムは個人情報および業務情報を扱うため、特に 個人情報の漏えい防止を目的として、可能な限りのセキュリティ対策を講ずる。
本セクションでは、情報処理推進機構（IPA）の推奨原則および業界のベストプラクティスに基づくセキュリティ要件を示す。
また、開発方針として、確立されたセキュリティ基準やガイドラインを参照し、脆弱性とその対策を考慮した設計を行う。

### 3.1. インフラストラクチャおよび運用セキュリティ
#### 3.1.1. アプリケーションセキュリティ
- API セキュリティ
システムは Django および Django REST Framework（DRF）が提供する組み込みのセキュリティ機能を活用し、一般的な脅威から API を保護する。
  - 認証: 機密データを扱うすべての API エンドポイントに対して、強固な認証を適用する。
    - 主要方式：DRF Simple JWT を利用した JWT ベース認証（ステートレス API 用）
    - 管理画面：：Django Session Authentication（サーバレンダリング/管理者用エンドポイント）＋ CSRF 保護
    - アクセストークンの有効期限：30 日
    - 自動ログアウト：Web サイトで 30 分間操作がない場合、自動的にログアウト
  - 権限管理: API エンドポイントへのアクセスは、細分化された権限モデルを用いて厳格に制御する。
    - DRF の permission classes および 最小権限の原則に基づく RBAC（ロールベースアクセス制御）を適用
    - クライアント側の検証に関わらず、サーバ側の検証が最終的に優先される
    - Cloudflare による事前検証を経てアプリケーションにデータが渡される
  - 入力バリデーション: すべての入力データを厳格に検証し、インジェクション攻撃を防止し、データ整合性を確保する。
    - DRF serializer による型、長さ、フォーマット、範囲、列挙値などの検証
    - SQL インジェクション対策：すべての DB アクセスは Django ORM（パラメータ化クエリ）経由で実行
    - XSS（クロスサイトスクリプティング）対策：
      - サーバー側でレンダリングされるテンプレートは、Django の自動エスケープ機能により XSS を防止する。
      - クライアントアプリケーション（Angular）は、Angular の組み込みサニタイズ機能を利用して不正なスクリプトの実行を防止する。
      - さらに、Content Security Policy（CSP） を適用することで、XSS のリスクをさらに低減する。

#### 3.1.2. ネットワークセキュリティ
- ファイアウォール構成：アプリケーションは AWS 上にデプロイされており、複数レイヤーのセキュリティグループと VPC のネットワーク分割によって保護されている。
  - ALB へのインバウンドは HTTP/HTTPS（80/443）のみ許可し、Cloudflare の出口 IP 範囲に限定。
  - ALB はプライベートサブネット上の ECS タスク（ポート8000）へ転送。タスクはデータサブネット内でのみ RDS（5432）および Redis（6379）へアクセス可能。
  - ワークロードからの外向き通信は必要な宛先（例：443番ポートの外部サービス）に限定。
- 侵入検知／防止：AWS GuardDuty を有効化し、継続的な脅威検知と異常監視を実施。検出された内容はインシデント対応手順に基づきトリアージされる。
  - AWS GuardDuty による継続的な脅威検知および異常監視を実施。
  - S3 保護、ECS・EC2ランタイム監視エージェント、S3およびEC2のマルウェア防御、RDSログイン監視を含む。
- VPC：VPC は以下の 3 層構造で構成され、複数の AZ（アベイラビリティゾーン）に分散されている。
  - 公開層（ALB/NAT）、アプリ層（ECSタスク）、データ層(RDS/ElastiCache）を分離した三層構成。
  - 公開サブネットには ALB と NAT Gateway のみ配置し、アプリ／データ層は非公開経路のみ許可。
- Cloudflare セキュリティサービス：Cloudflare はネットワーク防御の主要レイヤーとして使用されている。
  - Web Application Firewall（WAF）：Cloudflare Managed Rulesets および OWASP CRS（Core Rule Set） を有効化して、Web アプリケーションファイアウォールを構成する。
  - DDoS 緩和対策：Cloudflare のグローバルネットワークによる 常時 DDoS 防御 を利用。
  - TLS/SSL暗号化：ACM証明書を使用して、クライアントからCloudflare、およびCloudflareからAWS ALBへの接続にHTTPSを強制する。
    - 推奨 Cloudflareモード：エンドツーエンドで有効な証明書を要求する「Full (Strict)」。
    - 最小　TLSバージョンは 1.2 以上。
  - Bot 管理：ボット検知を有効化し、credential stuffing や scraping を低減。

#### 3.1.3. データ保護
- データ暗号化
  - 通信中の暗号化（In Transit）
    - すべての外部エンドポイントで TLS 1.2 以上を使用
    - HSTS（HTTP Strict Transport Security） を強制
    - サービス間通信（例：データベース接続の SSL など）がサポートされている場合は 内部通信も暗号化
  - 保存時の暗号化（At Rest）
    - RDS のスナップショット／インスタンス：KMS による暗号化を有効化
    - S3 バケット：SSE-KMS / SSE-S3 を使用
    - ECS タスクの基盤となるブロックストレージも暗号化
    - アプリケーションシークレットは AWS Secrets Manager に保存し、コードにベタ書きしない
  - 情報（PII）の暗号化（PII Data Encryption）
    - 対象範囲：顧客に関連する個人情報（例）
      - 氏名、フリガナ
      - 電話番号、メールアドレス
      - 住所
      - 生年月日
      - 免許証番号
      - 会員 ID
    これらを機微情報（Sensitive Data） として扱う
    - 方法
      - 機微データはアプリケーションレベルでフィールド暗号化
      - 保存時に平文として読めない状態を保証
      - 一部のフィールドでは キー付きハッシュインデックス により平文を使わずに検索（照合）が可能
    - 鍵管理
      - 暗号鍵／秘匿情報は AWS Secrets Manager に保存
      - キーは最小権限 IAM で実行時にアプリへ注入
      - コードやコンテナイメージ内に埋め込まない
      - 顧客データの暗号鍵は 6か月ごとにローテーション
    - アクセス制御・ログ
      - 復号処理は 認可されたサーバ側プロセスのみに限定
      - 復号操作はログに記録し、監査可能とする
    - 保存期間と削除
      - PII は業務上／法令上必要な期間のみ保持し、期間後は 削除または匿名化する
      - バックアップおよびリカバリー
- バックアップ戦略（Backup Strategy）
  - RDS：自動バックアップを 毎日取得（保持期間 7 日）
  - RDS のスナップショットは 大阪リージョンへクロスリージョン複製
  - S3 バケットも大阪リージョンへ複製し、耐久性を確保
#### 3.1.4. セキュリティと性能の整合性
データ暗号化によって、システムの性能に大きな影響は発生しない

### 3.2. 脆弱性管理
当システムでは、AWS Inspector、AWS GuardDuty、Security Hub、AWS Config などの AWS セキュリティサービスを活用し、セキュリティ体制を継続的に監視・管理。
主な脆弱性管理手順は以下のとおり。

- パッチ管理
  - EC2 インスタンス：CVSS スコア 7.0 以上の脆弱性 またはベンダー推奨のアップデートに対し、パッチマネージャーを使用した毎月の自動パッチ適用を実施。
  - 開発ツール：脆弱性修正版がリリースされた際に、毎月自動更新。
  - RDS：重大な脆弱性がある場合、保守スケジュール内で minor / major アップデートを自動適用。
- アプリケーションフレームワークの脆弱性管理
  - Django、Angular、Swift：CVE スコア 7.0 以上 または公式の緊急パッチがリリースされた場合、フレームワーク本体および依存ライブラリを 毎月（必要に応じて随時）更新。
  - 使用ツール：AWS Inspector、npm audit、pod outdatedなどを用いて脆弱性を定期的に確認。
- Cloudflare セキュリティ
  - WAF／ファイアウォールルール：新たな脅威検出に基づき、毎月（および緊急時）更新。本番反映前に開発環境で検証を実施。
  - TLS／証明書更新：有効期限の 60日前 に、証明書を自動更新。年次更新も自動管理。
- 脆弱性情報の収集
AWS 公式サイト／RSS から 毎週 AWS Security Bulletin を収集し、新しい脆弱性情報が公開された際に迅速に対応。
- AWS Inspector
  - 重大な脆弱性が検出された場合、ECS コンテナイメージ、EC2 インスタンスに対して脆弱性一覧を提示
  - コンテナイメージの脆弱性： 該当ライブラリや依存パッケージの手動アップデートが必要
  - EC2 インスタンス：パッチマネージャーにより自動パッチ適用
- 暗号鍵管理
  - 暗号情報暗号化鍵は Secrets Manager により管理され、6か月ごとにローテーションを実施。

## 4. UI 要件
### 4.1. デザイン
#### 4.1.1. iPad UI デザイン
#### 基本原則
- レイアウト
  - UI は iPad の画面サイズ（横向き）に適応するものとする。
  - 必要に応じて、1 カラム／複数カラムレイアウトの間でコンテンツが自動的に再配置される。
  - 文脈に応じた選択操作には ポップオーバーや フォームシートを使用し、Slide Over / Split View など マルチタスクに対応できる設計とする。
  - 顧客登録・車両登録などの複雑なワークフローでは、フルスクリーンモーダルを使用し、データの一貫性を確保する。
- Web UI（バックオフィス業務）
  - サービスはバックオフィス担当者向けに PC Web ブラウザで提供される。
  - 画面左側には、ロール（Admin、Shop Manager、Dealer Manager など）に応じた モジュールアクセスを制御する折りたたみ式サイドバーを配置。
  - Bootstrap を用いた グリッドレイアウトにより、固定幅のサイドバー、レスポンシブなメインコンテンツ領域を構成。
  - ダッシュボードはカードベースの UI コンポーネントを使用し、サービス、タブレットユーザー、車両、ショップ、注文履歴など主要機能へのクイックアクセスを提供。
  - ロールに応じて表示内容が変わる 可視性制御を実装。
- 操作性と到達性
主要アクションボタンは、iPad 横向き利用時に親指が自然に届く下部または右側（後端） に配置する。また、ノッチやシステムジェスチャーとの干渉を避けるため、セーフエリアを適切に考慮して UI を配置する。
- 画面スペースの有効活用
  - 複数カラムのグリッドや リスト／テーブルレイアウトを活用して情報を効率的に提示。
  - 大規模データセットやフォームでは、セクション分割されたパネル、スクロールに追従するヘッダーを使用して読みやすさと比較のしやすさを向上。
  - 不要な余白を避けることで、画面スペースを最大限有効活用。

#### フロントエンドコンポーネントの機能要件

| フロントエンドコンポーネント | 業務価値 | 主要要件 |
|----|----|----|
| ナビゲーション & コンテナ | 一貫したナビゲーションパターンにより、研修時間と操作ミスを削減。スタッフが必要な機能を素早く見つけられ、平均作業時間を短縮。UI を再構築することなく新しいサービスカテゴリを追加でき、事業拡大に柔軟に対応。 | ● ブランドのプライマリカラーを使用した一貫したトップナビゲーション ● 明確なタイトルと文脈に応じたアクション ● モーダルパターン：迅速な作業向けのスライドインシート、長いワークフロー向けのフルスクリーンモーダル ● セーフエリアと Split View / Slide Over に対応したレイアウト |
| ボタン | 重要な操作への案内が明確になることで コンバージョン率が向上。ボタンの階層化により、ユーザーが操作を途中放棄するケースを減少。注文確定などの高リスク操作では、破壊的アクションを明確にすることで 重大ミスを防止。 | ●	Primary / Secondary / Tertiary の明確な階層 ●	押下、無効、破壊的操作などの視覚状態 ● 最低 44×44 pt の押下領域 ●	ローカライズ可能で簡潔なラベル ● タイプスケールに沿ったテキスト ●	最小サイズを維持しつつ動的に幅を調整 |
| テキスト入力 & ピッカー | 日本語・国際入力仕様に合わせたバリデーションによって 入力ミスを削減。車両固有データ（ナンバー、金額、電話番号など）の データ精度向上。自動車サービス特有の選択項目に最適化したピッカーにより 業務効率を向上。日本語入力特有の数字・通貨フォーマットを適切に処理し データ整合性を確保。 | ●	ラベル付き入力欄、インラインバリデーション、必須マーク ● 日本語入力対応 ● リスト、範囲、日付用のアンカー付きポップオーバーピッカー ● データ形式に応じたキーボードタイプを使用 |
| リスト & グリッド | 商品の可視性と比較性が向上し、在庫回転率を高める。車両履歴や部品互換性への素早いアクセスにより サービス時間を短縮。構造化された情報表示とフィルタリングにより データドリブンな意思決定を支援。 | ● 商品、車両、注文向けの高密度リスト／グリッド ● 選択、文脈アクション、フィルタリングをサポート ●	交互の行背景や sticky header で可読性向上 |
| モーダル & ポップオーバー | 文脈を維持して操作できるため ワークフローの中断を防止。必要な情報に集中でき、認知負荷を軽減。顧客登録など複雑な操作を最後まで完了させることでデータ整合性が向上。 | ●	選択肢：ポップオーバー ● 複数ステップ：シート ● Cancel／Close を明示 ● 安全な場合は外側タップで閉じる ●	ポップオーバーは呼び出しコントロールにアンカーされる |
| カレンダー & スケジューリング | 日／週／月ビューを使ったピット管理で ベイ稼働率を最大化。青（通常）、オレンジ（手動）の色分け表示により 予約の衝突を防止。予約密度や作業配分の可視化で店舗運営を改善。工場 → ピット → 日時 の ステップ式予約で顧客対応を効率化。預かり・返却を含む複数日サービスも明確に可視化。 | ● 月／週カレンダーで容量・ステータスを表示 ● 日付・時間枠の選択、期間移動、予約作成／編集 ● 選択不可状態の明確なフィードバック |
| PDF生成 & プレビュー | 標準化された文書により 見積承認までの時間を短縮。サービス内容が明確に示され 見積承認率が向上。重い処理もコールバックパターンで UIブロックを回避。外部リソース（署名・画像）も成功／失敗コールバックで管理。PDF 生成は通常 2秒以内。 | ●	見積・請求・点検など印刷用PDFを生成／プレビュー ● 一貫した表レイアウト、ヘッダーシェーディング ● システム共有／印刷との統合 ●	署名画像や写真を埋め込み可能 |
| 署名入力 | 標準化されたデジタル署名により 承認遅延を削減。顧客署名、車両預かり／返却など様々なワークフローに対応。各文書タイプで一貫した署名処理パターン。顧客署名が得られない場合の 署名スキップオプション。PDF へ直接署名を埋め込み 記録の完全性を確保。 | ●	署名キャンバス（Clear/Undo あり） ●	保存された署名を PDF／レシートに挿入 ●	高コントラストの筆跡と十分なキャンバスサイズ |
| カラー選択 | 標準カラーパレットにより 車両識別ミスを防止。10種類の標準色で識別が簡単（黒、青、茶、金、銀、緑、赤、黄、白、その他）。車両記録全体で色名称が一貫。ローカル市場向けに 色名の日本語化にも対応。 | ● ラベル付きスウォッチパレット（明るい色には枠線） ● アンカー付きポップオーバーで表示 ● 選択時に即座にフィールドへ反映 |
| QRスキャン | 即時識別とレコード取得により チェックイン時間を短縮。直接読み取りにより 入力ミスを排除。車両履歴・推奨サービスにリンクし サービス精度を向上。普通車・超軽・軽自動車など複数のQR形式に対応。エラー時には自動的に「再試行」ポップアップを表示。 | ● カメラベースのスキャン（色付きオーバーレイで視覚フィードバック） ● 車種別 QR タイプ切替のセグメントコントロール ● 成功／失敗処理を備えた非同期処理 ● 基本的なエラーメッセージ（将来的なガイド拡張に対応） |
| メール／印刷ワークフロー | 状況に応じた文書選択で 顧客コミュニケーションを効率化。注文状況に応じて適切な文書を選択（新規：見積、完了：作業完了報告）。メールアドレス検証や複数宛先対応で 受信者管理を改善。非同期の文書生成で UI ブロックを防止。ロゴ・署名など外部リソースも基本的なエラー回復あり。 | ●	テンプレート／ファイル／受信者選択 ● 必須項目のバリデーション ● システム共有／印刷で送信 ● 全ラベル／メッセージはローカライズ可能 |

#### 4.1.2. ブランドとスタイル
- ロゴの使用
公式ロゴアセット（ライト／ダーク背景用、モノクロバージョン）を提供すること。ロゴの最小サイズおよびクリアスペース（余白）ルールを明確に定義する。
  - ロゴの配置場所については以下を指定すること
  - アプリ起動時（スプラッシュ画面）
  - アプリのトップバー（ナビゲーションバー）
  - PDF 文書（見積書、請求書など）
  - メール本文および署名部分

  
  <img width="348" height="47" alt="image" src="https://github.com/user-attachments/assets/58b2195d-c30c-4101-86d9-923d2d01029c" />

- PDF（作業関連書類）
見積書、作業開始報告書、作業完了報告書、点検報告書、チェックアップシートなど、ほとんどの運用PDFには以下の一般データが含まれる。
  - ドキュメント番号：各PDFを一意に識別し、追跡・照合・監査を可能とする。
  - 販売者情報：文書に責任を持つ法的主体（支店／整備工場）を明確にし、正しい税務管轄やブランド表示を保証する。
  - 顧客情報：契約当事者を特定し、承認・責任・支払条件を明確にする。
  - 車両情報：正しい車両（ナンバープレート、車種など）が見積もり・整備・認証されることを保証する。
  - 明細項目（ラインアイテム）：部品・作業内容、数量・時間、単価、税金・割引の内訳を明確にする。
  - 合計金額：承認や支払いのための財務サマリー（小計、税額、総合計）をまとめる。
  
  ※異なる帳票間では、主に文言やデザインの軽微な変更のみが行われる。
  
  <img width="165" height="38" alt="image" src="https://github.com/user-attachments/assets/553e0616-9bec-40ff-abcf-0d43c0ac2cb3" />

  <img width="372" height="302" alt="image" src="https://github.com/user-attachments/assets/13ec8889-9021-4a54-833f-39aaf6fed573" />

- カラーパレット
  - プライマリ：#204178（R32 G65 B120）ナビゲーションバー、主要アクション、選択状態に使用。
  - ライトプライマリ：#284074（R40 G64 B116）ホバー時の強調表示やセカンダリ強調に使用。
  - セカンダリグレー：#E0E0E0　区切り線、ニュートラル背景、セカンダリコントロールに使用。
  - ターシャリ背景：#F4F5F7　画面の背景やセクションの控えめな背景に使用。
  - App ブラック / App グレー：#212121（本文テキスト） / #606060（セカンダリテキスト）
  - 無効状態：#555555　無効化されたテキストやアイコンに使用。
  - 表ヘッダー／サブヘッダー：#C0CBDF / #C0CBF1（60% 不透明度）　セクションヘッダーや交互行の背景に使用。
- タイポグラフィ
  - 主要書体は BIZ UDPGothic ファミリー（Regular / Bold） を使用し、日本語とラテン文字の両方に適したフォントとする。
  - 見出し（Heading）は Bold、統一されたタイプスケールを使用。
  - 本文テキストおよびフォームラベルは Regular を使用。
  - Dynamic Type をサポートし、テキストを画像として埋め込むことは避ける。
  - カスタムフォントが利用できない環境では システムUIフォントをフォールバックとして使用する。
- トーン & ボイス
  - プロフェッショナルで明確、丁寧な日本語を優先するユーザー体験を提供し、必要に応じて英語も利用可能。
  - ラベルやメッセージは、簡潔でわかりやすく行動を促す表現で画面や PDF 全体で一貫性を持たせることを基本とする。

### 4.2. 具体的な UI 要件
- テキスト
  - フォントファミリー：日本語／英語の可読性を最大化するため、BIZ UDPGothic Regular / Bold を使用する。
  - Static Type（固定テキストサイズ）：文字サイズは常に固定で、アクセシビリティ設定によって変化しない。
  - ローカライズ（Localization）：すべてのユーザー向けテキストは 日英ローカライズ対応（JA/EN）。日付・数値のハードコーディングを避け、ロケールに応じた形式を使用し、日本語の組版規則（スペース、句読点など）を遵守する。
- レイアウト
  - ナビゲーション
    - プライマリブランドカラーを使ったトップナビゲーションを常時表示
    - 戻る操作と文脈アクションは画面右側に配置
    - バーは背後のコンテンツに対して常に十分な可読性を保つ
  - グリッド & リスト
    - サービス選択では 複数カラムのグリッドを使用
    - カレンダーでは色分けされたスケジュールセルを使用
    - テーブル／リストは交互の行背景と明確な列ヘッダーを使用
  - ポップオーバー & シート
    - 短いリストやピッカーには ポップオーバーを使用（呼び出し元にアンカー）
    - 大きい入力や複数ステップには フォームシートを使用
    - 適切な場合は外側タップで閉じる。必ず明示的な閉じる／キャンセルを提供
  - フォーム
    - 関連項目をグルーピング
    - 必須マークの表示
    - インラインバリデーション対応
    - 幅が狭い画面では多段カラムフォームを避ける
  - PDF / 印刷
    - 生成される PDF は画面上の構造を反映
    - 明確なセクションヘッダー、表、署名画像を埋め込み可能
  - 国際化
    - 日英いずれの文字数にも対応する柔軟なラベル幅
    - 改行ありのマルチライン表示
    - 日本語の長いラベルに対応する左右パディング設定
  - パフォーマンス
    - 遷移中の重い処理は遅延させる（defer）
    - ポップオーバー／シート表示中にメインスレッドをブロックしないこと
  - テキストのレスポンシブ処理
    - ラベル：適切な折り返しによる複数行サポート
    - ボタン：最小サイズを保ちながら動的幅
    - テーブル：横スクロール対応、必要に応じて列固定（sticky columns）
    - ツールチップ：切れたテキストに対して展開可能なヘルプテキスト
    - 省略ルール（Ellipsis）：タップ／ホバーで全文表示
  - 包括的メッセージシステム
    - 成功：黒テキスト(成功しました)、自動で閉じる
    - 警告：黒テキスト、行動指示を含む、手動で閉じる
    - エラー：赤テキスト、明確な説明 + 再試行オプション、手動で閉じる

## 5. 環境要件
### 5.1. 前提条件
- システムは 2 つの構成環境を持つものとする
- 開発環境：システム構築（開発・検証）に使用
- 本番環境：実際の運用で使用

### 5.2. 環境区分
- 開発環境
  - 開発者が実装および検証を行うためのシステム構築用（開発）環境。
  - ローカル開発では、Docker Compose を使用してアプリケーション、PostgreSQL、Redisのコンテナ化サービスを利用。.env ファイルで環境依存の設定を管理する。
  - 開発者が実装・テストを行う環境で問題が発生しても、本番サービスに影響はない。
  - 公開エンドポイント：cta.c0d1um.io（HTTPS）。
  - CI/CD パイプラインは GitLab Runner を使用。
  - 利用ブランチ：main（ビルド、テスト、デプロイ）。
- 本番環境
  - ユーザーが実際に操作する運用環境。
  - AWS サービス（ALB、ECS、RDS） を使用。
  - 公開エンドポイント：etrcars.jp（HTTPS）。
  - CI/CD パイプラインは GitLab Runner を使用。
  - 利用ブランチ：terraform-prod（ビルド、テスト、デプロイ）。

### 5.3. サーバ構成

- インフラ構成図
<img width="600" height="730" alt="image" src="https://github.com/user-attachments/assets/e9182594-1bf8-49e9-981d-0bc5b5d57c9f" />

- クラウドプラットフォームとリージョン
  - 当システムは Amazon Web Services（AWS） 上にホストされており、プライマリリージョンは ap-northeast-1（東京）。
  - ディザスタリカバリ（DR）は、重要サービス（例：データベーススナップショット、S3 レプリケーション）をap-northeast-3（大阪）へクロスリージョン複製する設計に基づいている。
  - Primary（本番稼働）：ap-northeast-1（東京）
  - Secondary/DR（災害対策）：ap-northeast-3（大阪）
- ネットワーク構成
  - VPC 構造
  VPC は以下のサブネットに分割されている
    - パブリックサブネット（ingress）
    - アプリケーションサブネット（ECS サービス）
    - データサブネット（データベース／キャッシュ）
  - Application Load Balancer（ALB）
    - ALB は HTTPS（443）を終端し、アプリケーションサービスへ転送する。
    - ALB のアクセスログは 監査のため S3 に保存される。
  - Security Groups（セキュリティグループ）
    - 受信（ingress）は信頼されたネットワークに制限される。
    - ALB へのパブリック ingress は Cloudflare の IP レンジなどに限定。
    - 東西（east-west）通信／内部通信は必要なポートのみ許可。
      - ALB → ECS：8000
      - ECS → RDS：5432
      - ECS → Redis：6379
  - Outbound Connectivity（外向き接続：NAT Gateway & VPC エンドポイント）
    - NAT Gateway がパブリックサブネットに配置され、プライベートサブネット（例：Django アプリ）からの 外部 API へのアウトバウンド通信を可能にする。
      - 利用例：ETRBUY API、Google Vision API
    - これにより、アプリケーションは外部へ接続できるが 外部からの受信（inbound）は遮断されるため安全。
  - VPC Endpoints（VPC エンドポイント）
    - Interface VPC Endpoints を利用するサービス
      - Amazon ECR（コンテナイメージの安全なプル）
      - AWS Secrets Manager（アプリケーションシークレット取得）
    - これにより ECS タスクはプライベートサブネット内からインターネットを経由せずに AWS サービスへ安全にアクセスできる。
    - すべての通信が AWS 内部ネットワーク内で完結し、セキュリティが強化される。
- コンテナオーケストレーション
  - AWS Elastic Container Service（ECS） + Fargate により、アプリケーションワークロードを実行している。
  - サービス構成
    - Web API／アプリケーション（Django）
      - ALB 配下で稼働
      - 可用性確保のため Desired Count = 3 タスク を基本とする
    - バックグラウンドワーカー（RQ）
      - 非同期処理用に 最低 1 タスク を稼働
  - オートスケーリング（Target Tracking Policies）
    - CPU使用率のターゲット：約 50%
    - メモリ使用率のターゲット：約 75%
    - Web サービスの Desired Count
      - 最小 3、最大 8
  - タスクログ（Web API／アプリ & Background Worker）
    - Amazon CloudWatch Logs に送信し、集中管理とトラブルシューティングを可能にする。
  - CloudWatch Logs の保持期間（無期限）
    - アプリケーション／ワーカーの ECS タスクロググループは「保持期限なし（Never Expire）」 に設定
    - 長期間のトラブルシューティングや監査に利用可能
    - コストが問題となる場合は、ログを S3 へエクスポートし、S3 側でライフサイクル管理を行うことも可能
  - 長時間実行タスクの CloudWatch モニタリング
    - オペレーターは CloudWatch Logs を用いて、Background Worker（RQ） および Web タスクのログを確認できる
    - ECS Task ID ごとの Log Stream にアクセス
    - CloudWatch Logs Insights を利用して
      - 特定のジョブ／タスク ID でフィルタ
      - 開始／ハートビート／終了ログの確認
      - 所用時間の分析

      これにより、サーバへログインすることなく長時間処理のトレースが可能

  - Runtime
    - AWS Fargate
    - アプリケーションは Gunicorn で提供
    - Backend 基盤イメージ：python:3.11.13
    - Frontend：Angular 16 でビルド

- データサービス
  - リレーショナルデータベース（RDS）
    - Amazon RDS for PostgreSQL（バージョン 16.8）を使用
  - Multi-AZ 構成
    - RDS インスタンスは Multi-AZ 構成でデプロイされており、可用性の確保と、AZ 障害発生時の 自動フェイルオーバーに対応
  - キャッシュ／キュー（Redis）
    - Amazon ElastiCache for Redis を使用し、キャッシュ処理およびキュープロセッシングをサポート。
  - オブジェクトストレージ（S3）
    - 静的ファイル／メディアファイルを Amazon S3 に保存し、大阪リージョンへのクロスリージョンレプリケーションを構成することで耐障害性（レジリエンス）を確保。

- シークレットおよび設定管理
  - アプリケーションシークレット（例：データベース URL、暗号化鍵）は、AWS Secrets Manager に保存され、実行時に ECS タスクへ注入される。
  - 暗号化鍵は、年 1 回（手動）でローテーションを実施。
  - Secrets Manager への API コール（例：GetSecretValue）は、すべてAWS CloudTrail により記録され、完全な監査性とセキュリティ監視を確保する。
  - 機密情報以外の設定は、環境変数、およびパラメータ化されたタスク定義で提供される。

- ログ、監視、セキュリティ運用
  - ECS タスク／アプリケーションログ
    - Amazon CloudWatch Logs に送信・保持。
  - ALB アクセスログ
    - S3 バケット etrcars-alb-access-logs に保存し、ライフサイクル管理ポリシーを適用。
  - 脅威検知／脆弱性評価
    - AWS GuardDuty：脅威検知
    - AWS Inspector：脆弱性診断

    ※詳細は運用手順書（Operational Procedures）を参照

  - アラート通知（Alerting）

  CloudWatch Alarms が以下などの重要メトリクスに対して設定されている
    - CPU使用率
    - メモリ使用率
    - HTTP 5xx エラーレート
    - ECS タスク失敗
    - キュー深度（Queue Depth）

  オペレーターは CloudWatch アラームコンソールおよびダッシュボードを週次レビュー時に確認する。

  - インシデント対応プロセス（Incident Response Process）
    - 重大度レベル（SEV-1 / SEV-2 / SEV-3）を定義
    - SLA に基づくトリアージ
    - インシデントコマンダー（Incident Commander）を任命
    - インシデントタイムラインを記録
    - ステークホルダーへの状況報告を実施

  - 変更管理、リリース管理、バックアップ検証、インシデント対応は以下の文書で定義
  
  documentation/project document/AWS Operation Document.md

  - CloudWatch Alarm Rules
  
  アラームが発生した場合、will@codium.co、および  Sano@sbrandg.comに通知が送信されるアラームルールは以下のとおり。

| アラーム名 | 発火条件 |
|----|----|
| IAM Policy Changed | IAMMonitoring ≥ 1 が 1 分間に 1 データポイント |
| RootAccountLogin | RootAccountLogin ≥ 1 が 5 分間に 1 データポイント |
| Network Change | NetworkConfigModificationCount > 0 が 1 分間に 1 データポイント |
| Session manager connected | CallCount ≥ 1 が 5 分間に 1 データポイント |
| RDS CPU | CPUUtilization ≥ 50 が 1 分間に 1 データポイント |
| RDS Memory | FreeableMemory ≤ 1073741824（≒1GB） が 1 分間に 1 データポイント |
| etrcars-django-service-AlarmLow-Mem | MemoryUtilization < 67.5 が 15 分間に 15 データポイント |
| etrcars-django-service-AlarmLow-CPU | CPUUtilization < 45 が 15 分間に 15 データポイント |
| etrcars-django-service-AlarmHigh-Mem | MemoryUtilization > 75 が 3 分間に 3 データポイント |
| etrcars-django-service-AlarmHigh-CPU | CPUUtilization > 50 が 3 分間に 3 データポイント |

- 継続性：スケーラビリティとキャパシティ管理

ユーザー数の増加に伴い、処理負荷が増大することを前提として、その拡張に対応するための設備および方法を以下に示します。なお、ネットワーク機器（回線・ファイアウォール）は対象外とする。

| 項目 | 方針 |
|----|----|
| Scale in/out（スケールイン/スケールアウト） | アーキテクチャは ECS/Fargate のオートスケーリング機能を前提として構築されている。ターゲットトラッキング：CPU 約 50%、メモリ 約 75%。Web サービスの desired count は 最小 3、最大 8。大規模イベントや需要急増が予測される場合は、事前にスケールアウトを実施する。 |
| Scale up/down（スケールアップ/スケールダウン） | Auto Scaling によって タスク数／インスタンス数が継続的に上限に達する場合、タスクサイズまたは AWS サービスクォータの引き上げを検討する。長時間・高負荷のバッチ処理がある場合は、一時的にリソースをスケールアップして処理時間を短縮する。 |

RDS データベースおよび Redis キャッシュサービスについては、水平スケーリング（Horizontal Scaling）は実装されておらず、垂直スケーリング（Vertical Scaling）のみが適用対象となります。

- バックアップおよび災害復旧
  - データベースバックアップ
    - データベースに対して 自動および手動スナップショットを取得する。
    - 手順に基づき、RDS スナップショットの定期的な検証を実施する。
  - S3 ライフサイクル管理（ログ）
    - ライフサイクルポリシーにより、ALB アクセスログやアプリケーションログ（エクスポート）などのログオブジェクトを7 年後に Amazon S3 Glacier へ移行する。
    - 自動削除は行わず、オブジェクトは保持される（no auto-deletion）。
    - 同一ポリシーは 大阪リージョンのレプリカバケットにも適用される。
  - クロスリージョンレプリケーション（CRR）
    - Recovery Point Objective（RPO）：目標 < 1 時間
      - RDS
        - 自動 RDS スナップショットは 1 時間ごとに ap-northeast-3（大阪）へコピーする。
        - 最悪の場合でも、データ損失は 1 時間以内に抑えられる。
      - S3
        - S3 のクロスリージョンレプリケーション（CRR）は通常 数分以内に完了するため、静的アセットのデータ損失は最小限となる。
    - Recovery Time Objective（RTO）：目標 < 4 時間
      - 検知と判断：0〜30 分

      障害発生を検知し、DR チームがフェイルオーバー実行の承認を得るまでの時間。

      - インフラ切替：1〜2 時間
        - 大阪リージョンで RDS スナップショットをプライマリに昇格する自動スクリプトを実行。
        - 大阪リージョンで ECS サービスを本番レベルまでスケールアップ。
        - Route 53 の DNS を更新し、etrcars.jp を大阪の ALB へ向ける。
      - DNS 伝播と検証：1〜1.5 時間
        - DNS 変更がグローバルに伝播するのを待つ。
        - アプリケーションが完全に機能していることを確認するため、包括的なヘルスチェックを実行する。
  - データ整合性検証ジョブ（Data Consistency Verification Jobs）
    - RDS スナップショットレプリケーション監視
      - Action（対応）

      Cross-region レプリカの ReplicationLag を監視する CloudWatch アラームを導入、もしくは大阪へのスナップショットコピー失敗時にアラートを発火させる。

      - Outcome（効果）

      レプリケーション停止が即座に通知され、インフラチームが迅速に対応できる。

    - S3 レプリケーションステータス監視
      - Action（対応）
        - S3 Replication Time Control（S3 RTC） を有効化。
        - 「15 分以内に 99.99% のオブジェクトが複製されない場合」にCloudWatch アラームを発火。

      - Outcome（効果）

      重要な静的アセットが常に DR リージョンへ正しく複製され、障害時に大阪リージョン側で確実に利用可能となる。

### 5.4. クライアント要件
本システムに対応するクライアント環境（デバイス、OS、ブラウザ）は以下の通りとする。
○: テスト対象ブラウザ
△: ブラウザはテストされない（一部のスタイルが壊れている可能性があります）
×: 動作保証外

| デバイス | デバイスタイプ | ブラウザ | 画面 | 表示タイプ |
|----|----|----|----|----|
| PC | Windows | Edge (v110+) | ○ | PC |
| PC | Windows | Chrome (v110+) | ○ | PC |
| PC | Windows | Firefox (v110+) | ○ | PC |
| PC | Mac | Safari (v15.4+) | ○ | PC |
| PC | Mac | Chrome (v110+) | ○ | PC |
| PC | Mac | Firefox (v110+) | ○ | PC |
| Tablet | iPad (iOS 12+) | Safari | ○ | Tablet |

## 6. 機能要件
### 6.1. 機能要件一覧
このセクションでは、IPA（情報処理推進機構）の指針に沿った高レベルの機能要件を定義する。各要件には一意の識別子が付与され、優先度および要件内容が明確に記述されます。

#### 6.1.1. ユーザーの役割と権限
要件名： RBAC：ロールベースアクセス制御

優先度：高

説明：

1. システムは RBAC を実装し、ユーザーが自身のロールで許可された操作およびデータのみにアクセスできるようにする。
2. RBAC の適用範囲と強制事項：

RBAC は、すべての API エンドポイント、iPad / Web UI の操作、必要に応じた バックグラウンドタスク、データエクスポート操作に適用される。

また、個人識別情報（PII）はロールポリシーに応じて マスキングまたは非表示とする。

(例：Administrator：全モジュールにアクセス可能（※顧客情報は非表示）Dealer / Shop Manager：全モジュール＆顧客情報の表示が許可)

3. 認証・トークンポリシー（JWT）：
- ユーザーセッションは JWT により管理し、最大有効期間は 30 日とする。
- 有効期限が切れた場合、ユーザーは再ログインが必要とする。
- 現時点では サーバ側のトークン無効化（revocation）は未対応とする。

(有効な無効化手段は トークンの有効期限切れとなる)

4. アクティビティ監査：

認証イベント、ロールの割り当て／変更、そして機微操作へのアクセスは、追跡性とコンプライアンスを確保するために記録されるものとする。

機能動作の詳細については 「機能：アクティビティログと監査証跡」 を参照。また、記録データの 保持期間やアクセス制御については、セキュリティ要件を参照。

#### 6.1.2. システムの役割

| 役割 | 権限 |
|----|----|
| Administrator（管理者） | 顧客情報を除く すべてのモジュールへアクセス可能 |
| Dealer / Shop Manager（ディーラー／ショップマネージャー） | 顧客情報を含めた すべてのモジュールへアクセス可能 |
| Shop（ショップ） | ショップユーザー向けの ショップ設定を管理 |
| Service Staff（サービススタッフ） | エンドユーザーに対応するための iPad 操作を担当 |
| Factory User（工場ユーザー） | 修理予約情報の閲覧・管理 |

#### 6.1.3. ロール別機能権限一覧

| No. | 機能 | 管理者 | ディーラー／ショップマネージャー | ショップ | サービススタッフ | 工場ユーザー |
|----|----|----|----|----|----|----|
| 1 | ユーザー & ロール管理 | 作成、編集、閲覧、削除 | 編集、閲覧、削除 | × | × | × |
| 2 | 顧客管理 & 車両管理 | × | 作成、編集、閲覧、削除 | × | × | × |
| 3 | サービスカタログ管理 | 作成、編集、閲覧、削除 | 作成、編集、閲覧、削除 | × | × | × |
| 4 | 予約 & キャパシティ管理 | × | 閲覧 | × | 作成、編集、閲覧、削除 | 作成、編集、閲覧、削除 |
| 5 | 注文 & 注文アイテム管理 | × | 閲覧 | × | 作成、編集、閲覧、削除 | × |
| 6 | 車両点検ワークフロー | × | 閲覧 | × | 作成、編集、閲覧、削除 | × |
| 7 | リマインダー | 閲覧、編集 | 閲覧、編集 | × | 作成、編集、閲覧、削除 | × |
| 8 | QRコード操作 | × | × | × | 作成 | × |
| 9 | アンケート & フィードバック | 作成、編集、閲覧、削除 | 作成、編集、閲覧、削除 | × | 編集、閲覧 | × |
| 10 | データインポート／エクスポート | 作成、閲覧 | 作成、閲覧 | × | × | × |
| 11 | 分析 & レポート | 閲覧 | 閲覧 | × | × | × |
| 12 | 車両事前点検 & レポート | × | × | × | 作成、編集、閲覧、削除 | × |
| 13 | アクティビティログ & 監査証跡 | 閲覧 | × | × | × | × |
| 14 | ショップ管理 | × | × | 作成、編集、閲覧、削除 | × | × |

#### 6.1.4. システム機能・ユースケース
##### 通常フロー図

<img width="339" height="411" alt="image" src="https://github.com/user-attachments/assets/06b34f9c-563f-46da-a3fb-b2110f216918" />

- 入力バリデーションの一般方針
入力バリデーション内容：

バリデーションは 「Database Entity Document（Excel）」で定義されたデータ型および制約に従う。

以下が含まれる：
  - データ型
  - 桁数／精度
  - 必須／NULL 可
  - 許可される値（enum）
  - フォーマット（例：メール、電話番号）
  - 一意性
  - 外部キーの存在チェック

- バリデーションレイヤー：
  - クライアント側：基本的なチェックとガイダンスを提供
  - サーバ側（DRF serializers / models）：すべての入力に対して最終的な判断を行う、クライアントのチェック有無に関わらず 必ずサーバ側で完全検証する

- エラーハンドリング & UX：
  - 入力がエンティティ定義と矛盾している場合、API は 400 Bad Request を返し、フィールドレベルの構造化されたエラーを返却
  - UI では、対象フィールドの近くに インラインエラーを表示。画面全体に影響するエラーは、非ブロッキングのアラート／ダイアログでサマリーを提示。
  - メッセージは 日本語／英語（JA/EN） ローカライズ対応

- 一貫性とセキュリティ：
  - すべての入力は サーバ側でサニタイズ & バリデーション
  - 大量インポートも同様に完全検証
  - 無効な行／ファイルは 拒否され保存されない

##### ユーザー & ロール管理における入力バリデーション例
役割：管理者、ディーラー/ショップマネージャー、ショップ

| フィールド | タイプ | バリデーション |
|----|----|----|
| フルネーム | テキスト | - 必須入力 - 255文字未満 |
| ユーザー名 | テキスト | - 必須入力 - 一意 - 255文字未満 |
| メール | テキスト | - 必須入力 - 一意 - メール形式 |
| 役割 | 選択肢 | - 必須入力 - 選択肢は管理者、ディーラーマネージャー、ショップマネージャー、ショップ |
| ショップ | 選択肢 | - 必須入力 - システム内のすべての店舗から選択 |

※システムはパスワード設定用のメールをユーザーに送信する。

役割：サービススタッフ

| フィールド | タイプ | バリデーション |
|----|----|----|
| フルネーム | テキスト | - 必須入力 - 255文字未満 |
| ユーザー名 | テキスト | - 必須入力 - 一意 - 255文字未満 |
| パスワード | テキスト | - 必須入力 - 6文字以上 - 英数字（alphanumeric）で構成 - Username と同一でないこと |
| パスワード確認 | 選択肢 | - パスワードと完全一致 |
| ショップ | 選択肢 | - 必須入力 - システム内のすべての店舗から選択 |

役割：工場ユーザー

| フィールド | タイプ | バリデーション |
|----|----|----|
| フルネーム | テキスト | - 必須入力 - 255文字未満 |
| ユーザー名 | テキスト | - 必須入力 - 一意 - 255文字未満 |
| パスワード | テキスト | - 必須入力 - 6文字以上 - 英数字（alphanumeric）で構成 - Username と同一でないこと |
| 工場 | 選択肢 | - 必須入力 - システム内のすべての工場から選択 |

- ユーザーおよびロール管理
  - 機能名：アカウントおよびロール管理
  - 優先度：高
  - 利用者：管理者
  - 説明：

  ユーザーアカウント、ロール、および権限を管理し、iPad ・ Web アプリケーションにおける 最小権限アクセスを徹底する。これにより、迅速で監査可能なオンボーディングを可能にし、不正なデータアクセスを防止する。

  - 前提条件：管理者が認証済みであること
  - 主な成功シナリオ（基本フロー）：
    - 管理者（Admin）がユーザーアカウントを 新規作成または編集する。
    - 管理者がユーザーに役割と適用範囲（例：ショップ／拠点） を割り当てる。
    - システムは変更内容を保存し、ユーザーへ メール通知を送信する。

  - エラーシナリオ：
    - ユーザー名が重複している
    - 確認用パスワードが一致しない

  - 事後条件
  ユーザーアカウントが 指定されたロールで正常に作成／更新される。対応する 操作ログが記録される。

- 顧客管理および車両管理
  - 機能名：顧客登録および管理
  - 優先度：高
  - 利用者：ディーラー／ショップマネージャー
  - 説明：顧客情報、連絡先設定、サービス履歴、ナンバープレート情報などを管理し、顧客アカウントに紐づける。
  - 前提条件：権限を持つユーザーが 認証済みであること
  - 主な成功シナリオ（基本フロー）：
    - ユーザーが顧客プロフィールを作成または更新する。
    - システムが連絡先情報や顧客の希望条件をバリデートする。
    - 顧客情報および車両情報をインポートする。

- サービス／商品カタログ管理
  - 機能名：サービスおよび商品のメンテナンス
  - 優先度：中
  - 利用者：Administrator、Dealer / Shop Manager
  - 説明：サービスや商品の価格設定、適用条件などを含むサービス／商品カタログを管理・更新する。
  - 前提条件：権限を持つユーザーが認証済みであること
  - 主な成功シナリオ（基本フロー）：
    - ユーザーがサービス／商品定義をインポートまたは更新する。
    - システムが価格情報や表示属性をバリデートする。

- 予約・キャパシティ管理
  - 機能名：キャパシティ制御付きサービス予約
  - 優先度：高
  - 利用者：Dealer / Shop Manager、Service Staff、Factory User
  - 説明：ショップ／工場のキャパシティに応じた予約スケジュールを管理し、過剰予約を防止し、待ち時間やリードタイムを削減する。
  - キャパシティおよび空き状況に関するルール
    - 日別キャパシティ設定
      - 工場（Factory）／メカニックごとに1 日あたりのサービス対応可能数、および1 日あたりの対応可能車両数をWeb 管理サイト上で日単位に設定できる。
      - これらの設定値は POS（iPad）での予約機能にそのまま適用される。
    - 日別制限
      - ある日の 確定済み予約数が上限タッチ数、または上限台数に達した場合、その日は新規予約ができない状態となる。
      - POS（iPad）は該当日を 選択不可として無効化する、または「予約不可」として表示する。
    - 時間枠（スロット）空き状況チェック
      - リクエストされた各時間枠に対して、iPadは、定義済みの予約スロットおよび既存の予約を確認する
      - 時間枠に空きがあり、かつ新しい予約によって、上限タッチ数、または上限台数の制限を超えない場合のみ、予約が受け付けられる。
    - リアルタイム検証とフィードバック
  
    POS（iPad）は予約確定前に リアルタイムでキャパシティチェックを実施する。

    - もしキャパシティ上限またはスロット制限に達している場合は、予約はブロックされ、理由を説明するローカライズ済みメッセージ（日本語／英語）が表示される。
    - カレンダー更新

    予約が正常に完了すると、カレンダービュー、当日のキャパシティカウンターが即時更新され、その日の残りの空き状況がリアルタイムに反映される。

  - 前提条件：顧客および車両データが存在する。キャパシティおよび予約枠が設定済みである。
  - 主な成功シナリオ（基本フロー）：
    - ユーザーがショップ／工場、日付、時間、サービスを選択する。
    - システムが枠の空き状況とキャパシティ制約を確認する。
    - 予約が確定し、カレンダーに反映される。

- 注文および注文アイテム管理
  - 機能名：注文および商品
  - 優先度：高
  - 利用者：Service Staff、Dealer / Shop Manager
  - 説明：作業注文を作成し、サービス／商品アイテム、割引、手数料などを含めて管理する。
  - 前提条件：サービスおよび商品データが存在している
  - 主な成功シナリオ（基本フロー）：
    - ユーザーが作業注文を作成し、商品アイテムを追加する。
    - システムが合計金額、税金、割引を算出する。
    - 注文が保存され、サービスワークフローの次段階へ進む。
  - ワークフロー：
<img width="77" height="246" alt="image" src="https://github.com/user-attachments/assets/58e68f1c-1f30-40f4-bf6c-731b6fcb430b" />

- 安全点検ワークフロー
  - 機能名：安全点検オーダー
  - 優先度：High（高）
  - 利用者：Service Staff、Dealer / Shop Manager
  - 説明：車両点検を標準化し、車両状態を記録することで顧客からの信頼性を高め、透明性のある推奨事項／見積りを生成し、サービス効率を改善する。
  - 前提条件：
    - 作業オーダーが存在していること
    - 車両が紐づいていること
  - 主な成功シナリオ（基本フロー）：
    - スタッフが点検ステージを設定し、点検内容を入力する。
    - ユーザーが作業オーダーを作成し、商品アイテムを追加する。
    - システムが合計金額・税金・割引を計算する。
    - オーダーが保存され、サービスワークフローが次の段階へ進む。

- リマインダー
  - 機能名：リマインダーダッシュボード
  - 優先度：高
  - 利用者：Dealer / Shop Manager、Service Staff
  - 説明：メンテナンスリマインダーを統合管理し、顧客のリテンション向上を図る。
  - 前提条件：購入履歴データが存在すること
  - 主な成功シナリオ（基本フロー）：
    - システムが Web サービスで設定されたルール（例：最終整備日）に基づきリマインダーを集計する。
    - リマインダー情報を iPad 上にステータス付きで表示する。

- QR コード操作
  - 機能名： QR コード読み取り
  - 優先度：中
  - 利用者：Service Staff
  - 説明：車両点検シートの QR を読み取り、車両情報を更新することで、受付時の入力作業を効率化し、入力ミスを削減する。
  - 前提条件：車両点検シートが用意されている
  - 主な成功シナリオ（基本フロー）：
    - システムが車両に紐づく QR コードを読み取る。
    - スタッフが iPad でコードをスキャンする。
    - システムがデータを保存／更新し、画面に表示する。

- アンケート・フィードバック
  - 機能名：アンケート配布・回収
  - 優先度：中
  - 利用者：Dealer / Shop Manager、Service Staff
  - 説明：顧客アンケートを作成・配布し、フィードバックを収集してサービス品質を向上させる。
  - 前提条件：顧客の連絡先および同意が取得済みであること
  - 主な成功シナリオ（基本フロー）：
    - ショップ／ディーラーマネージャーがアンケートを作成する。
    - サービススタッフが顧客の回答を入力する。
    - 回答が収集され、要約される。

- データインポート／エクスポート
  - 機能名：インポート／エクスポート
  - 優先度：中
  - 利用者：Administrator、Dealer / Shop Manager
  - 説明：構造化ファイルからマスタ／業務データをインポートし、分析用のレポートをエクスポートする。
  - 前提条件：
    - 正しくフォーマットされたファイル
    - マッピングルールが定義されていること
  - 主な成功シナリオ（基本フロー）：
    - ユーザーがインポート用ファイルをアップロード、またはエクスポートを要求する。
    - システムがスキーマをバリデートし、非同期で処理を行う。
    - 結果（成功／失敗、またはエクスポートファイル）がダウンロード可能となる。
    - 各タスクのタイムアウトは 3600 秒（＝1 時間）に設定され、タイムアウトに達した場合、そのタスクは 失敗として扱われ、記録される。

- 分析・レポート
  - 機能名：オペレーショナルダッシュボード／レポート
  - 優先度：中
  - 利用者：Administrator、Dealer / Shop Manager
  - 説明：予約、キャパシティ、注文、売上などの指標を可視化し、データドリブンな業務判断を支援する。
  - 前提条件：システムに必要なデータが存在すること
  - 主な成功シナリオ（基本フロー）：
    - ユーザーがレポートまたはダッシュボードをフィルタリングする。
    - システムがフィルタ条件に基づきデータを集計し表示する。
    - ユーザーは必要に応じてレポートをエクスポートする。

- 車検 & レポート
  - 機能名：車検およびレポート生成
  - 優先度：高
  - 利用者：Service Staff
  - 説明：
    - 作業開始前に車両状態を記録し、顧客の信頼性と作業効率を向上させるための機能。

    6 ステップの構造化されたチェックプロセスを実施し、予約スケジュールを作成し、レポート（PDF）を生成する。

  - 前提条件：車両が作成済みであること
  - 主な成功シナリオ（基本フロー）：
    - スタッフが点検ステップを順番に選択する。
    - スタッフが項目ごとに重大度や測定値を記録する。
    - スタッフが適切な商品アイテムを選択する。
    - システムが合計金額、税金、割引を計算する。
    - 点検オーダーが保存され、ステップごとに進行する。
    - システムが各ステップごとに確認用 PDF レポートを生成する。

- アクティビティログ／監査証跡
  - 機能名：ユーザー活動ログ & システム監査ログ
  - 優先度：高
  - 利用者：Administrator、Dealer / Shop Manager、Security
  - 説明：システム内のアクティビティログを改ざん不可（immutable）で記録し、検索・参照できるようにする。
  - 前提条件：ユーザーが認証済みであること
  - 主な成功シナリオ（基本フロー）：
    - システムが以下の情報を含むログを記録する：

    イベント種別、タイムスタンプ、利用者、ロール、操作内容、対象ユーザー、クライアント IP／デバイス、アプリバージョン、詳細データ
    
    - 管理者がディーラー名やアクター（氏名）でログを検索・フィルタリングする。

## 7. データベース要件
### 7.1. テーブル構成

本節では、システムで使用するデータベースのテーブル構成について定義する。

<img width="451" height="387" alt="image" src="https://github.com/user-attachments/assets/8f89af88-8506-4f30-8e05-26f57a548e51" />

### 7.2. テーブル定義

| No | テーブル名（物理名） | テーブル名（論理名） |
|----|----------------------|----------------------|
| 1 | branches_branch | 支店 |
| 2 | dealers_dealer | ディーラー |
| 3 | services_service | 商品カテゴリー |
| 4 | cars_cardata | 車両情報 |
| 5 | car_brands_brand | 車両_メーカー |
| 6 | car_models_model | 車両_車種 |
| 7 | car_models_submodel | 車両_車種(サブ) |
| 8 | license_plates_licenseplate | ナンバープレート |
| 9 | license_plates_todoliststatus | ステータス_Todoリスト |
| 10 | service_histories_servicehistory | 声掛けリスト/最終見積もりデータ |
| 11 | customers_customerinformation | 顧客情報 |
| 12 | customers_licenseplatecustomerinformation | 顧客・ナンバープレートのマッピングテーブル |
| 13 | customers_memo | 車両/顧客メモ |
| 14 | customers_phone | 使用していないテーブル |
| 15 | dealers_defaultregion | 地域名一覧 |
| 16 | shops_shop | 店舗(SS) |
| 17 | shops_staff | 店舗(SS)スタッフ |
| 18 | qr_codes_qrcode | 車検証QRデータ |
| 19 | qr_codes_qrcodelicenseplate | ナンバープレート・車検証QRデータのマッピングテーブル |
| 20 | reminders_reminder | 声掛けリスト/ディーラー別 |
| 21 | reminders_remindercondition | 声掛けリスト設定 |
| 22 | products_product | 商品 |
| 23 | product_views_productview | 商品グループ1・商品グループ2 |
| 24 | factories_factory | 工場 |
| 25 | factory_bookings_factorybookingorderitem | 工場予約の見積もりデータ |
| 26 | factory_bookings_factorybooking | 工場予約カレンダー |
| 27 | factory_bookings_factorybooking_mechanics | 工場予約カレンダー/工場スタッフ |
| 28 | factory_capacities_capacity | 工場予約カレンダー/キャパシティー設定 |
| 29 | orders_order | 見積もりデータ |
| 30 | order_items_orderitem | 保存されたアイテムデータ/カーメンテ見積 |
| 31 | order_items_subgroup | 保存されたアイテムデータ/カーメンテ見積 |
| 32 | order_items_repairimage | リペアキズ画像 |
| 33 | order_items_subgroupproduct | 保存されたアイテムデータ/車検見積 |
| 34 | user_profiles_userprofile | ログインユーザーに関する追加情報 |
| 35 | user_profiles_userprofile_shops | ログインユーザー・店舗のマッピングテーブル |
| 36 | auth_user | 認証ユーザー |
| 37 | order_items_carinspectionitem | 車検見積もりに紐づくPIグループ項目 |
| 38 | recommendations_recommendation | 基本６製品 適合 |
| 39 | factories_factory_shop | 工場・店舗のマッピングテーブル |
| 40 | products_carinpsectioncheckup | 車検Step2/事前点検項目 |
| 41 | products_carinpsectionpartfeeitem | 車検Step2/部品・作業料(PI番号別) |
| 42 | products_pigroupitem | 事前点検紐づけ番号(PI番号) |
| 43 | car_checkups_carcheckupitem | 使用されていないテーブル |
| 44 | booking_slots_factorybookingslot | リペア予約カレンダー/予約情報 |
| 45 | stages_stage | リペア予約カレンダー/工程進捗情報 |
| 46 | stage_logs_stagelog | リペア予約カレンダー/工程進捗ログ |
| 47 | customers_zipcodeaddress | 郵便番号データ |
| 48 | customers_mapcodeaddress | マップコードデータ |
| 49 | car_checkup_orders_carcheckuporder | 安全点検作成データ |
| 50 | car_checkup_order_items_carcheckuporderitem | 安全点検作成詳細データ |
| 51 | ios_deploy_iosapp | iOS/アプリ情報 |
| 52 | product_views_displayimage | 商品グループ1・2の画像データ |
| 53 | product_views_alacartecondition | 使用されていないテーブル |
| 54 | car_brands_carprice | 車両価格 |
| 55 | export_results_exportresult | エクスポート結果 |
| 56 | import_results_importresult | インポート結果 |
| 57 | announcements_announcement | お知らせ |
| 58 | orders_orderservice | 見積データに含まれるカテゴリーデータ |
| 59 | order_items_inspectionitem | オイル・タイヤ用作業完了確認項目データ |
| 60 | order_items_checkitem | 車検見積データ紐づくStep2項目のデータ |
| 61 | questionnaires_question | アンケート質問情報 |
| 62 | questionnaires_questionmenu | アンケートメニュー |
| 63 | questionnaires_questionmenuquestion | アンケートメニュー・アンケート情報のマッピングテーブル |
| 64 | questionnaires_questionchoice | アンケート選択肢情報 |
| 65 | questionnaires_answeritem | アンケート回答情報 |
| 66 | questionnaires_answer | アンケート/ユーザー回答情報 |
| 67 | user_profiles_mechanicstaff | 工場スタッフ |
| 68 | car_checkups_carcheckup | 安全点検 |
| 69 | user_profiles_mechanicstafffactoryuser | ログインユーザー・工場のマッピングテーブル |
| 70 | car_checkups_carcheckupcomment | 安全点検備考情報 |
| 71 | recommendations_recommendationremarks | 基本６製品 適合備考情報 |
| 72 | order_items_portablecanorderitem | 携行缶詰替え販売 |
| 73 | order_items_carinspectionorder | 車検見積で選択・入力されたデータ |
| 74 | orders_custodyorder | 預かり・代車・パーツ |
| 75 | order_items_custodyorderitem | パーツ発注で作成された商品データ |
| 76 | booking_slots_shopbooking | 見積データに紐づく予約データ |
| 77 | booking_slots_shopbookingslot | 自店店舗カレンダーで作成された予約データ |
| 78 | booking_slots_pit | 予約カレンダーのPIT情報 |
| 79 | orders_orderimage | 見積データに含まれる画像(署名)データ |
| 80 | order_items_carinspectionquestionnaire | 車検Step1問診 |
| 81 | reminders_remindermemo | 声掛けリスト/備考データ |
| 82 | shops_alertfilter | 使用されていないテーブル |
| 83 | shops_alertfilteroption | 使用されていないテーブル |
| 84 | custody_loan_lenders_compensationdetail | 代車貸出証発行/補償詳細 |
| 85 | custody_loan_lenders_ledger | 預かり・代車/帳票 |
| 86 | factories_inspectionfactory | 車検実施店 |
| 87 | license_plates_todolistsetting | Todoリスト設定 |

#### No.1 branches_branch（支店）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 | NULL | 備考 |
|------|------------------|------------------|---------|------|------|
| PK | id | ID | integer | NO | 主キー |
|  | created_at | 作成日時 | timestamp with time zone | NO |  |
|  | updated_at | 更新日時 | timestamp with time zone | NO |  |
|  | alive | 有効フラグ | boolean | NO |  |
|  | sequence | 並び順 | integer | NO |  |
|  | name | 支店名称 | varchar(255) | NO |  |
| FK | created_user_id | 作成ユーザーID | integer | NO | users.id |
| FK | updated_user_id | 更新ユーザーID | integer | NO | users.id |

#### No.2 dealers_dealer（ディーラー）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 | NULL | 備考 |
|------|------------------|------------------|---------|------|------|
| PK | id | ID | integer | NO | 主キー |
|  | created_at | 作成日時 | timestamp with time zone | NO |  |
|  | updated_at | 更新日時 | timestamp with time zone | NO |  |
|  | alive | 有効フラグ | boolean | NO |  |
|  | sequence | 並び順 | integer | NO |  |
|  | name | ディーラー名称 | varchar(255) | NO |  |
|  | tax_rate | 税率 | numeric(4,2) | NO |  |
|  | logo | ロゴ | varchar(100) | YES |  |
|  | is_production | 本番環境 | boolean | NO |  |
|  | is_approval_function | 承認機能 | boolean | NO |  |
|  | dealer_type | ディーラー種別 | smallint | NO |  |
| FK | branch_id | 支店ID | integer | NO | branches_branch.id |
| FK | created_user_id | 作成ユーザーID | integer | NO | users.id |
| FK | updated_user_id | 更新ユーザーID | integer | NO | users.id |
| FK | default_region_id | 地域名ID(初期値) | integer | YES | dealers_defaultregion.id |
|  | is_default_sort | 並び順(初期値)有効フラグ | boolean | NO |  |
|  | filter_config | Webサイト上のフィルター設定 | jsonb | YES |  |

#### No.3 services_service（商品カテゴリー）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | type | カテゴリーの種類を保存 | integer |
|  | name | 商品カテゴリー名称 | varchar(50) |
|  | image | 画像ファイルパス | varchar(100) |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |
| FK | dealer_id | ディーラーID | integer |
| FK | shop_id | 店舗ID | integer |
|  | repair_service_type | 使用されていない列 | boolean |

#### No.4 cars_cardata（車両情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | data_type | 使用されていない列 | integer |
|  | front_photo | 車両画像ファイルパス | varchar(100) |
|  | brand | メーカー名 | varchar(255) |
|  | model | 車種名 | varchar(255) |
|  | color | ボディーカラー | varchar(255) |
|  | color_code | ボディーカラーコード | varchar(255) |
|  | registered_date | 初年度登録年月 | date |
|  | expired_date | 車検満了日 | date |
|  | mileage | 現在の走行距離 | integer |
|  | oil_check_up | 使用されていない列 | date |
|  | tire_check_up | 使用されていない列 | date |
|  | battery_check_up | 使用されていない列 | date |
|  | car_inspection_check_up | 使用されていない列 | date |
|  | model_type | 車種種別 | integer |
|  | model_size | 車両サイズ | varchar(3) |
|  | ios_version | iOSバージョン | varchar(255) |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | license_plate_id | ナンバープレートID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |
|  | sub_model | 車種名(サブ) | varchar(255) |
|  | purchased_date | 購入日 | date |
|  | air_check_up | 空気圧点検日 | date |
|  | eneos_check_up | ENEOSアプリID取得日 | date |
|  | first_mileage | 最初の走行距離 | integer |
|  | line_check_up | LINE友だち登録日 | date |
|  | air_check_up_after | 空気圧/後 | numeric(4,1) |
|  | air_check_up_before | 空気圧/前 | numeric(4,1) |
|  | eneos_id | ENEOSアプリID | varchar(10) |
|  | end_leasing_date | リース契約終了日 | date |
|  | insurance_affiliated_store | 任意保険契約店舗 | integer |
|  | insurance_end_date | 任意保険契約終了 | date |
|  | insurance_start_date | 任意保険契約開始 | date |
|  | leasing_classification | リース顧客区分 | integer |
|  | leasing_dealer | リース特約店 | varchar(255) |
|  | leasing_shop | リース担当SS | varchar(255) |
|  | maintenance_pack | メンテナンスパック | integer |
|  | non_life_insurance | 任意保険損害保険会社 | varchar(255) |
|  | security_number | 任意保険証券番号 | varchar(255) |
|  | start_leasing_date | リース契約開始日 | date |
|  | tightening_check_up | 増し締めチェック登録日 | date |
|  | leasing_company | リース会社 | varchar(255) |

#### No.5 car_brands_brand（車両_メーカー）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | name | メーカー名称 | varchar(255) |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.6 car_models_model（車両_車種）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | name | 車種名称 | varchar(255) |
| FK | brand_id | メーカーID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.7 car_models_submodel（車両_車種(サブ)）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | name | 車種(サブ)名称 | varchar(255) |
| FK | model_id | 車種ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.8 license_plates_licenseplate（ナンバープレート）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | prefecture | 地域名 | varchar(100) |
|  | number | ナンバー | varchar(100) |
|  | classification | 分類番号 | varchar(10) |
|  | kana | ひらがな | varchar(10) |
|  | serial_number | 一連指定番号 | varchar(10) |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.9 license_plates_todoliststatus（ステータス_Todoリスト）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | name | ステータス名称 | varchar(255) |
|  | sequence | 並び順 | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.10 service_histories_servicehistory（声掛けリスト/最終見積もりデータ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | service_type | サービス種別 | integer |
|  | service_date | サービス実施日 | date |
|  | total_price | 合計金額 | numeric(12,2) |
|  | memo | メモ | text |
| FK | license_plate_id | ナンバープレートID | integer |
| FK | customer_id | 顧客ID | integer |
| FK | dealer_id | ディーラーID | integer |
| FK | shop_id | 店舗ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.11 customers_customerinformation（顧客情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | last_name | 姓 | varchar(255) |
|  | first_name | 名 | varchar(255) |
|  | last_name_kana | セイ | varchar(255) |
|  | first_name_kana | メイ | varchar(255) |
|  | birthday | 生年月日 | date |
|  | gender | 性別 | smallint |
|  | email | メールアドレス | varchar(255) |
|  | address | 住所 | varchar(255) |
|  | memo | メモ | text |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.12 customers_licenseplatecustomerinformation（顧客・ナンバープレートのマッピングテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
| FK | customer_id | 顧客ID | integer |
| FK | license_plate_id | ナンバープレートID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.13 customers_memo（車両/顧客メモ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | memo | メモ内容 | text |
| FK | customer_id | 顧客ID | integer |
| FK | car_id | 車両ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.14 customers_phone（使用していないテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | phone_number | 電話番号 | varchar(255) |
| FK | customer_id | 顧客ID | integer |

#### No.15 dealers_defaultregion（地域名一覧）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | name | 地域名 | varchar(100) |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.16 shops_shop（店舗(SS)）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | address_line_1 | 使用されていない列 | varchar(255) |
|  | address_line_2 | 使用されていない列 | varchar(255) |
|  | prefecture | 都道府県 | varchar(255) |
|  | town | 番地 | varchar(255) |
|  | city | 市区町村 | varchar(255) |
|  | building | 建物名 | varchar(255) |
|  | postal_code | 郵便番号 | varchar(255) |
|  | name | 店舗名 | varchar(255) |
|  | email | メールアドレス | varchar(255) |
|  | phone_number | 店舗電話番号 | varchar(75) |
|  | fax_number | 店舗ファックス番号 | varchar(75) |
|  | is_active | 店舗有効フラグ | boolean |
|  | is_production | 本番環境 | boolean |
|  | default_region | 地域名(初期値) | varchar(50) |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | dealer_id | ディーラーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |
| FK | inspection_factory_id | 車検工場ID | integer |
| FK | to_do_list_setting_id | ToDoリスト設定ID | integer |

#### No.17 shops_staff（店舗(SS)スタッフ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | active | 有効フラグ | boolean |
| FK | shop_id | 店舗ID | integer |
|  | family_name | 姓 | varchar(255) |
|  | sequence | 並び順 | integer |
|  | given_name | 名 | varchar(255) |
|  | name | スタッフの姓名 | varchar(255) |
|  | alive | 有効フラグ | boolean |

#### No.18 qr_codes_qrcode（車検証QRデータ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | qrcode_data | QRコードデータ | text |
|  | issued_at | 発行日時 | timestampwithtimezone |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.19 qr_codes_qrcodelicenseplate（ナンバープレート・車検証QRデータのマッピングテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
| FK | license_plate_id | ナンバープレートID | integer |
| FK | qrcode_id | 車検証QRデータID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.20 reminders_reminder（声掛けリスト/ディーラー別）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | reminder_type | リマインダー種別 | integer |
|  | target_date | 対象日 | date |
|  | memo | 備考 | text |
| FK | dealer_id | ディーラーID | integer |
| FK | license_plate_id | ナンバープレートID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.21 reminders_remindercondition（声掛けリスト設定）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | condition_type | 条件種別 | integer |
|  | condition_value | 条件値 | varchar(255) |
| FK | reminder_id | 声掛けリストID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.22 products_product（商品）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | sequence | 並び順 | integer |
|  | name | 商品名 | varchar(255) |
|  | price | 価格 | numeric(12,2) |
|  | tax_type | 税区分 | integer |
|  | cost | 原価 | numeric(12,2) |
|  | description | 商品説明 | text |
| FK | service_id | 商品カテゴリーID | integer |
| FK | dealer_id | ディーラーID | integer |
| FK | shop_id | 店舗ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.23 product_views_productview（商品グループ1・商品グループ2）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | view_type | 表示種別 | integer |
|  | name | 表示名 | varchar(255) |
|  | sequence | 並び順 | integer |
| FK | product_id | 商品ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.24 factories_factory

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | name | — | varchar(255) |
|  | email | — | varchar(255) |
|  | address | — | varchar(255) |
|  | touch_capacity | — | integer |
|  | car_capacity | — | integer |
|  | is_active | — | boolean |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |
|  | drop_off | — | smallint |
|  | pickup | — | smallint |
|  | mechanic_capacity | — | integer |
| FK | dealer_id | — | integer |

#### No.25 factory_bookings_factorybookingorderitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | order_item_type | — | integer |
|  | part_name | — | varchar(100) |
|  | will_be_fixed | — | boolean |
|  | position | — | varchar(255) |
|  | side | — | varchar(10) |
|  | type | — | varchar(10) |
|  | repair_images | — | jsonb |
|  | touch | — | integer |
|  | status | — | integer |
|  | completed_datetime | — | timestampwithtimezone |
| FK | booking_id | — | integer |
|  | actual_touch | — | integer |
|  | after_repaired_image | — | varchar(100) |

#### No.26 factory_bookings_factorybooking

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | status | — | integer |
|  | order_number | — | varchar(100) |
|  | shop_name | — | varchar(255) |
|  | branch_name | — | varchar(255) |
|  | dealer_name | — | varchar(255) |
|  | shop_comment | — | varchar(200) |
|  | mechanic_comments | — | text |
|  | start_date | — | date |
|  | end_date | — | date |
|  | drop_off | — | timestampwithtimezone |
|  | pickup | — | timestampwithtimezone |
|  | product_category_snapshot | — | text |
|  | alt_color_code | ALT | varchar(255) |
|  | color_code | — | varchar(255) |
|  | car_data | — | jsonb |
|  | shop_data | — | jsonb |
| FK | factory_id | — | integer |
| FK | reviewer_id | — | integer |
|  | staff_comment | — | text |
|  | actual_pick_up_datetime | — | timestampwithtimezone |
| FK | order_id | — | integer |
|  | memo | — | text |

#### No.27 `factory_bookings_factorybooking_mechanics`

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
| FK | factorybooking_id | — | integer |
| FK | mechanicstaff_id | — | integer |

#### No.28 factory_capacities_capacity

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | date | — | date |
|  | touch_capacity | — | integer |
|  | car_capacity | — | integer |
|  | mechanic_capacity | — | integer |
| FK | factory_id | — | integer |
| FK | mechanic_id | — | integer |

#### No.29 orders_order

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | data_type | — | integer |
|  | status | — | integer |
|  | order_number | — | varchar(100) |
|  | sales_datetime | — | timestampwithtimezone |
|  | customer | — | jsonb |
|  | customer_signature | — | varchar(100) |
|  | car_info | — | jsonb |
|  | suggestion_for_customer | — | text |
|  | staff_comment | — | text |
|  | staff_signature | — | varchar(100) |
|  | dropoff_datetime | — | timestampwithtimezone |
|  | pick_up_datetime | — | timestampwithtimezone |
|  | actual_pick_up_datetime | — | timestampwithtimezone |
|  | paid | — | boolean |
|  | deposit_amount | — | numeric(12,2) |
|  | mileage | — | numeric(12,2) |
|  | repair_factory_id | — | integer |
|  | repair_factory | — | varchar(255) |
|  | repair_start_date | — | date |
|  | repair_end_date | — | date |
|  | repair_slot_ids | — | integer[] |
|  | type | — | varchar(50) |
| FK | created_user_id | — | integer |
| FK | customer_info_id | — | integer |
| FK | license_plate_id | — | integer |
| FK | shop_id | — | integer |
| FK | staff_id | — | integer |
| FK | updated_user_id | — | integer |
|  | is_car_rental | — | boolean |
|  | total_price | — | numeric(12,2) |
|  | applied_date | — | timestampwithtimezone |
|  | car_inspection_receive_option | — | smallint |
|  | car_inspection_step | — | smallint |
|  | checking_staff_id | — | integer |
|  | first_expected_date | — | timestampwithtimezone |
|  | fixing_staff_id | — | integer |
|  | is_substitute_car | — | boolean |
|  | is_telephone | — | boolean |
|  | parking_fine_option | — | smallint |
|  | pre_inspection_date | — | timestampwithtimezone |
|  | second_expected_date | — | timestampwithtimezone |
|  | visit | — | smallint |
|  | work_completion_confirmation_items | — | jsonb |
|  | work_delivered_confirmation_items | — | jsonb |
|  | booked_staff | — | varchar(256) |
|  | work_complete_staff | — | varchar(256) |
|  | work_start_staff | — | varchar(256) |

#### No.30 order_items_orderitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | name | — | varchar(256) |
|  | product | — | jsonb |
|  | quantity | — | numeric(12,2) |
|  | price | — | numeric(12,2) |
|  | uuid | — | uuid |
|  | sequence | — | integer |
| FK | created_user_id | — | integer |
| FK | order_id | — | integer |
| FK | updated_user_id | — | integer |
|  | severity | — | integer[] |
|  | result | — | integer |

#### No.31 order_items_subgroup

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | name | — | varchar(256) |
| FK | car_inspection_item_id | — | integer |

#### No.32 order_items_repairimage

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | uuid | — | varchar(100) |
|  | repair_image | — | varchar(100) |
| FK | created_user_id | — | integer |
| FK | order_id | — | integer |
| FK | order_item_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.33 order_items_subgroupproduct

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | name | — | varchar(256) |
|  | quantity | — | numeric(12,2) |
|  | unit | — | varchar(100) |
|  | price | — | numeric(12,2) |
|  | category_name | — | varchar(100) |
| FK | sub_group_id | — | integer |
|  | product_type | — | integer |
|  | attribute | — | jsonb |
|  | etr_product_id | — | integer |
| FK | order_id | — | integer |
|  | product_id | — | integer |
|  | factory_check_status | — | integer |
|  | is_complete | — | boolean |

#### No.34 user_profiles_userprofile

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | given_name | — | varchar(255) |
|  | family_name | — | varchar(255) |
|  | phone_number_1 | — | varchar(255) |
|  | phone_number_2 | — | varchar(255) |
|  | email | — | varchar(255) |
|  | profile_picture | — | varchar(100) |
|  | customer_code | — | varchar(255) |
|  | image | — | varchar(100) |
|  | role | — | integer |
| FK | branch_id | — | integer |
| FK | created_user_id | — | integer |
| FK | dealer_id | — | integer |
| FK | updated_user_id | — | integer |
| FK | user_id | — | integer |
|  | attempt | — | integer |
|  | lock_time | — | timestampwithtimezone |
|  | ios_version | — | varchar(255) |
|  | password_at | — | date |

#### No.35 user_profiles_userprofile_shops

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
| FK | userprofile_id | — | integer |
| FK | shop_id | — | integer |

#### No.36 auth_user

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | password | — | varchar(128) |
|  | last_login | — | timestampwithtimezone |
|  | is_superuser | — | boolean |
|  | username | — | varchar(150) |
|  | first_name | — | varchar(150) |
|  | last_name | — | varchar(150) |
|  | email | — | varchar(254) |
|  | is_staff | — | boolean |
|  | is_active | — | boolean |
|  | date_joined | — | timestampwithtimezone |

#### No.37 order_items_carinspectionitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | header_name | — | varchar(256) |
|  | title_name | — | varchar(256) |
| FK | created_user_id | — | integer |
| FK | order_id | — | integer |
| FK | updated_user_id | — | integer |
|  | pi_number | — | integer |
|  | details | — | varchar(256) |
|  | item_name | — | varchar(256) |
|  | ledger_number | — | integer |

#### No.38 recommendations_recommendation

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | model_number | — | varchar(100) |
|  | recommendation_type | — | varchar(10) |
|  | recommendation_data | — | jsonb |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |
|  | classification_number | — | varchar(255) |
|  | specification_number | — | varchar(255) |
|  | product_code | — | varchar(100) |
|  | remarks | — | text[] |
|  | brand | — | varchar(100) |
|  | car_size | — | varchar(4) |
|  | car_type | — | varchar(30) |
|  | model | — | varchar(100) |
|  | model_year | — | date |
|  | model_year_end | — | date |

#### No.39 factories_factory_shop

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
| FK | factory_id | — | integer |
| FK | shop_id | — | integer |

#### No.40 products_carinpsectioncheckup

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | sequence | — | integer |
|  | name | — | varchar(255) |
|  | attribute | — | jsonb |
|  | input_type | — | integer |
|  | pi_number | — | integer |
| FK | created_user_id | — | integer |
| FK | dealer_id | — | integer |
| FK | parent_id | — | integer |
| FK | pi_group_id | — | integer |
| FK | service_id | — | integer |
| FK | shop_id | — | integer |
| FK | updated_user_id | — | integer |
|  | ledger_number | — | integer |

#### No.41 products_carinpsectionpartfeeitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | pi_number | — | integer |
|  | name | — | varchar(255) |
|  | price | — | integer |
|  | unit | — | varchar(100) |
|  | attribute | — | jsonb |
|  | product_type | — | integer |
|  | service_type | — | integer |
| FK | created_user_id | — | integer |
| FK | pi_group_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.42 products_pigroupitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | group_name | — | varchar(100) |
|  | pi_number | — | integer |
|  | item_name | — | varchar(100) |
|  | details | — | varchar(100) |
|  | ledger_number | — | integer |
|  | sequence | — | integer |

#### No.43 car_checkups_carcheckupitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
| FK | created_at | — | timestampwithtimezone |
| FK | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | name | — | varchar(255) |
|  | price | — | numeric(12,2) |
|  | type | — | integer |
|  | attribute | — | jsonb |
|  | created_user_id | — | integer |
|  | updated_user_id | — | integer |

#### No.44 booking_slots_factorybookingslot

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | booking_slot_status | — | integer |
|  | booking_slot_type | — | smallint |
|  | shop_name | — | varchar(255) |
|  | slot_date | — | date |
|  | touches | — | integer |
|  | device_uuid | — | uuid |
| FK | factory_id | — | integer |
| FK | factory_booking_id | — | integer |

#### No.45 stages_stage

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | stage_number | — | smallint |
| FK | mechanic_id | — | integer |
| FK | order_item_id | — | integer |

#### No.46 stage_logs_stagelog

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | log_event | — | integer |
|  | created_at | — | timestampwithtimezone |
| FK | stage_id | — | integer |

#### No.47 customers_zipcodeaddress

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | zipcode | — | varchar(100) |
|  | region | — | varchar(100) |
|  | prefecture | — | varchar(100) |
|  | city | — | varchar(100) |
|  | town | — | varchar(100) |
|  | building | — | varchar(100) |

#### No.48 customers_mapcodeaddress

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | prefecture | — | varchar(100) |
|  | town | — | varchar(100) |
|  | prefecture_code | — | varchar(10) |
|  | town_code | — | varchar(10) |
|  | town_code_extension | — | varchar(10) |
|  | searchable_code | — | varchar(15) |

#### No.49 car_checkup_orders_carcheckuporder

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
| FK | created_user_id | — | integer |
| FK | customer_id | — | integer |
| FK | license_plate_id | — | integer |
| FK | order_id | — | integer |
| FK | shop_id | — | integer |
| FK | staff_id | — | integer |
| FK | updated_user_id | — | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | status | — | integer |
|  | customer_snapshot | — | jsonb |
|  | car_data_snapshot | — | jsonb |
|  | license_plate_snapshot | — | jsonb |
|  | checkup_datetime | — | timestampwithtimezone |
|  | attribute | — | jsonb |

#### No.50 car_checkup_order_items_carcheckuporderitem

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | name | — | varchar(256) |
|  | attribute | — | jsonb |
|  | result | — | integer |
|  | input_datetime | — | timestampwithtimezone |
|  | input_text | — | text |
|  | comment | — | text |
|  | alacarte_name | — | varchar(100) |
| FK | checkup_order_id | — | integer |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.51 ios_deploy_iosapp

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | name | — | varchar(50) |
|  | title | — | varchar(20) |
|  | app_type | — | integer |
|  | build_version | — | varchar(20) |
|  | version | — | varchar(20) |
|  | plist | — | varchar(100) |
|  | ipa | — | varchar(100) |
|  | release_note | — | varchar(255) |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |
|  | is_active | — | boolean |

#### No.52 product_views_displayimage

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | type | — | integer |
|  | image | — | varchar(100) |
|  | attribute | — | jsonb |
|  | product_view_hash | — | varchar(255) |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.53 product_views_alacartecondition

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | name | — | varchar(254) |
|  | x_position | — | numeric(10,7) |
|  | y_position | — | numeric(10,7) |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.54 car_brands_carprice

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | registered_year | — | integer |
|  | katashiki | — | varchar(100) |
|  | minimum_price | — | numeric(20,2) |
|  | maximum_price | — | numeric(20,2) |
| FK | created_user_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.55 export_results_exportresult

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | file | — | varchar(100) |
|  | export_type | — | varchar(100) |
|  | export_status | — | integer |
| FK | created_user_id | — | integer |
| FK | dealer_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.56 import_results_importresult

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|---|---|---|---|
| PK | id | ID | integer |
|  | created_at | — | timestampwithtimezone |
|  | updated_at | — | timestampwithtimezone |
|  | alive | — | boolean |
|  | file | — | varchar(100) |
|  | import_type | — | varchar(100) |
|  | file_info | — | jsonb |
|  | status | — | boolean |
|  | error_message | — | text |
|  | import_status | — | integer |
|  | error_messages | — | jsonb |
| FK | created_user_id | — | integer |
| FK | dealer_id | — | integer |
| FK | updated_user_id | — | integer |

#### No.57 announcements_announcement（お知らせ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | title | タイトル | varchar(255) |
|  | body | 本文 | text |
|  | published_at | 公開日時 | timestampwithtimezone |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.58 orders_orderservice（見積データに含まれるカテゴリーデータ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | sequence | 並び順 | integer |
| FK | order_id | 見積ID | integer |
| FK | service_id | 商品カテゴリーID | integer |

#### No.59 order_items_inspectionitem（オイル・タイヤ用作業完了確認項目データ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | item_type | 項目種別 | integer |
|  | is_checked | チェック有無 | boolean |
| FK | order_item_id | 見積商品ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.60 order_items_checkitem（車検見積データ紐づくStep2項目のデータ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | check_type | チェック種別 | integer |
|  | check_value | チェック値 | varchar(255) |
| FK | order_item_id | 見積商品ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.61 questionnaires_question（アンケート質問情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | question_text | 質問文 | text |
|  | question_type | 質問種別 | integer |
|  | sequence | 並び順 | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.62 questionnaires_questionmenu（アンケートメニュー）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | created_at | 作成日時 | timestampwithtimezone |
|  | updated_at | 更新日時 | timestampwithtimezone |
|  | alive | 有効フラグ | boolean |
|  | name | メニュー名 | varchar(255) |
|  | description | 説明 | text |
|  | sequence | 並び順 | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.63 questionnaires_questionmenuquestion（アンケートメニュー・アンケート情報のマッピングテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | sequence | 並び順 | integer |
| FK | questionmenu_id | アンケートメニューID | integer |
| FK | question_id | アンケート質問ID | integer |

#### No.64 questionnaires_questionchoice（アンケート選択肢情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | choice_text | 選択肢文言 | varchar(255) |
|  | sequence | 並び順 | integer |
| FK | question_id | アンケート質問ID | integer |

#### No.65 questionnaires_answeritem（アンケート回答情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | answer_text | 回答内容 | text |
| FK | question_id | アンケート質問ID | integer |
| FK | answer_id | アンケート回答ID | integer |

#### No.66 questionnaires_answer（アンケート/ユーザー回答情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | answered_at | 回答日時 | timestampwithtimezone |
| FK | user_id | 回答ユーザーID | integer |

#### No.67 user_profiles_mechanicstaff（工場スタッフ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | スタッフ名 | varchar(255) |
|  | qualification | 資格 | varchar(255) |
|  | active | 有効フラグ | boolean |

#### No.68 car_checkups_carcheckup（安全点検）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | checkup_date | 点検日 | date |
|  | checkup_type | 点検種別 | integer |
|  | result | 点検結果 | integer |
| FK | car_id | 車両ID | integer |
| FK | mechanic_id | 工場スタッフID | integer |

#### No.69 user_profiles_mechanicstafffactoryuser（ログインユーザー・工場のマッピングテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
| FK | user_id | ログインユーザーID | integer |
| FK | factory_id | 工場ID | integer |

#### No.70 car_checkups_carcheckupcomment（安全点検備考情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | comment | 備考 | text |
| FK | car_checkup_id | 安全点検ID | integer |

#### No.71 recommendations_recommendationremarks（基本６製品 適合備考情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | remark | 備考 | text |
| FK | product_id | 商品ID | integer |

#### No.72 order_items_portablecanorderitem（携行缶詰替え販売）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | quantity | 数量 | integer |
| FK | order_id | 注文ID | integer |

#### No.73 order_items_carinspectionorder（車検見積で選択・入力されたデータ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | inspection_step | 車検ステップ | integer |
|  | input_value | 入力値 | text |
| FK | order_id | 見積ID | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.74 orders_custodyorder（預かり・代車・パーツ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | order_type | 注文種別 | integer |
|  | memo | 備考 | text |
|  | ledger_number | 帳票番号 | integer |
|  | sequence | 並び順 | integer |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.75 order_items_custodyorderitem（パーツ発注で作成された商品データ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | 使用されていない列 | varchar(255) |
|  | price | 価格 | numeric(12,2) |
|  | type | 使用されていない列 | integer |
|  | attribute | 使用されていない列 | jsonb |
| FK | created_user_id | 作成ユーザーID | integer |
| FK | updated_user_id | 更新ユーザーID | integer |

#### No.76 booking_slots_shopbooking（見積データに紐づく予約データ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | booking_slot_status | 各予約枠のステータス | integer |
|  | booking_slot_type | 各予約枠の種別 | smallint |
|  | shop_name | 店舗名 | varchar(255) |
|  | slot_date | リペア作業開始日 | date |
|  | touches | 各項目のタッチ数 | integer |
|  | device_uuid | iPadの一意識別子 | uuid |
| FK | factory_id | 工場ID | integer |
| FK | factory_booking_id | 予約に関連するID | integer |

#### No.77 booking_slots_shopbookingslot（自店店舗カレンダーで作成された予約データ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | slot_start_time | 予約開始時刻 | timestampwithtimezone |
|  | slot_end_time | 予約終了時刻 | timestampwithtimezone |
|  | capacity | 受入可能数 | integer |
|  | status | 予約枠ステータス | integer |
| FK | shop_id | 店舗ID | integer |
| FK | pit_id | PIT ID | integer |

#### No.78 booking_slots_pit（予約カレンダーのPIT情報）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | PIT名称 | varchar(255) |
|  | sequence | 並び順 | integer |
|  | active | 有効フラグ | boolean |
| FK | shop_id | 店舗ID | integer |

#### No.79 orders_orderimage（見積データに含まれる画像(署名)データ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | image_path | 画像パス | varchar(255) |
|  | image_type | 画像種別 | integer |
| FK | order_id | 見積ID | integer |

#### No.80 order_items_carinspectionquestionnaire（車検Step1問診）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | answer_value | 回答内容 | text |
| FK | order_id | 見積ID | integer |
| FK | question_id | 問診質問ID | integer |

#### No.81 reminders_remindermemo（声掛けリスト/備考データ）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | memo | 備考 | text |
| FK | reminder_id | 声掛けリストID | integer |

#### No.82 shops_alertfilter（使用されていないテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | 使用されていない列 | varchar(255) |
| FK | shop_id | 店舗ID | integer |

#### No.83 shops_alertfilteroption（使用されていないテーブル）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | 使用されていない列 | varchar(255) |
| FK | alert_filter_id | アラートフィルターID | integer |

#### No.84 custody_loan_lenders_compensationdetail（代車貸出証発行/補償詳細）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | coverage_type | 補償種別 | integer |
|  | coverage_amount | 補償金額 | numeric(12,2) |
| FK | ledger_id | 帳票ID | integer |

#### No.85 custody_loan_lenders_ledger（預かり・代車/帳票）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | ledger_number | 帳票番号 | integer |
|  | sequence | 並び順 | integer |

#### No.86 factories_inspectionfactory（車検実施店）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | 工場名 | varchar(255) |
|  | active | 有効フラグ | boolean |

#### No.87 license_plates_todolistsetting（Todoリスト設定）

| PK/FK | カラム名（物理名） | カラム名（論理名） | データ型 |
|------|------------------|------------------|---------|
| PK | id | ID | integer |
|  | name | 設定名称 | varchar(255) |
|  | sequence | 並び順 | integer |
|  | active | 有効フラグ | boolean |






























  












































































































































































